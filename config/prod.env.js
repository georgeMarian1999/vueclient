'use strict';



module.exports = {
    NODE_ENV: '"production"' ,

    HAS_PROFESSIONAL_MODULE: true, // Tells if the app includes Professional sections (when TRUE), or Pathfinder only (when FALSE).

    // API paths
    API_BASE_URL: '"pro/v2.3"', // No trailing slash!
    API_BASE_URL_PATHFINDER: '"api/v2.3"', // No trailing slash!
    API_BASE_URL_AUTH: '"pro/v2.3"', // No trailing slash!
    API_BASE_URL_LOGOUT: '"pro/v2.3"', // No trailing slash!
}
